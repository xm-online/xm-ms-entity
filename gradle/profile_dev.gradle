
dependencies {
    implementation "org.postgresql:postgresql"
    testImplementation "org.testcontainers:postgresql"
    developmentOnly "org.springframework.boot:spring-boot-docker-compose"
}

ext {
    springProfiles = "dev"
    if (project.hasProperty("no-liquibase")) {
        profiles += ",no-liquibase"
    }
}

springBoot {
    buildInfo {
        excludes = ['time']
    }
}

bootRun {
    args = ["--spring.profiles.active=${springProfiles}"]
    systemProperty 'user.timezone', 'UTC'
    environment 'POSTGRES_PORT', '0'
}


processResources {
    inputs.property('version', version)
    inputs.property('springProfiles', springProfiles)
    inputs.property('logbackLoglevel', 'DEBUG')
    filesMatching("**/application.yml") {
        filter {
            it.replace("@project.version@", version)
        }
    }
    filesMatching("**/bootstrap.yml") {
        filter {
            it.replace("@spring.profiles.active@", springProfiles)
        }
    }
    filesMatching("**/logback-spring.xml") {
        filter {
            it.replace("#logback.loglevel#", "DEBUG")
        }
    }
}
