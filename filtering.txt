System have
/_search-with-typekey-and-template/xm-entities
AND
/_search-with-typekey/xm-entities
AND
/xm-entities/{entityTypeKey}/{idOrKey}/links/{linkTypeKey}/search
endpoints.

This endpoint search by elastic search.
But required installations without elastic search.

We need the next 4 endpoints:

1 - search by typeKey and full text query search by field "name" and field "description" in entities + some field from data field.
With paging and sorting.
Is it possible to create a full text search index for field "name" and "description" in DB by flag in entity spec?
Maybe create a separate table where we save only entities that have in entity spec flag textSearch: true?
+ fullTextSeachDataFields: [data.order, data.subObject.position] etc.
And process this save from hibernate listener?
Recomment best way to do this.
Sorting have to support jsonb fields like data.order, or data.subObject.position.
Remember to make fallback to oracle implementation.
1.1 - purpose with examples to add to this method filtering by jsonb fields.
I want jhipster like filtering by all fields and for Postgres by jsonb fields.
/Users/serhii.senko/work/XM/xm-ms-balance/src/main/java/com/icthh/xm/ms/balance/service/dto/BalanceCriteria.java
I want filter like
filter: {
 data.order.in: [1,2,3],
 data.subObject.position.eq: 5
}


2 - reuse same service method create api for search entity for "link add dialog", handle unique flag in spec
2.1 - add api to search and filter links for source entity id by link typeKey

3 - api to search by jpql template, templates can be defined in jpql-templates.yml or in jpql-templates/*.yml
method get and post have to be supported
named params have to look up in
for get: query params
for post: body fields

